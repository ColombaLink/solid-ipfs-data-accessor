import { createUriAndTermNamespace } from '@solid/community-server';

// eslint-disable-next-line @typescript-eslint/naming-convention
export const IPFS = createUriAndTermNamespace('http://ipfs.io/ns/ipfs#', 'cid');
